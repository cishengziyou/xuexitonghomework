---
name: chaoxing-scanner
description: 登录学习通，自动发现用户所有课程，扫描未交作业并分类
metadata:
  type: skill
  version: 1.0.0
  author: claude-code
---

# 学习通作业扫描器

登录学习通，自动发现用户所有课程，扫描未交作业，分类哪些 AI 能做、哪些需要自己动手。

## 功能特点

- ✅ 自动登录（支持账号密码和扫码）
- ✅ 自动发现所有课程
- ✅ 获取真实课程名（避免显示URL片段）
- ✅ 结构化+文本双重解析（兼容不同版本）
- ✅ 过滤已提交作业（"待批阅"状态）
- ✅ AI工作量估算
- ✅ 反爬虫机制应对
- ✅ 调试模式支持
- ✅ 会话复用（避免重复登录）

## 使用方法

### 方式1：作为skill调用

```bash
/chaoxing-scanner
```

### 方式2：命令行执行

```bash
# 基本用法
bash skill.sh

# 指定账号密码
bash skill.sh -p 13213732826 -d mypassword

# 仅发现课程
bash skill.sh -m discover

# 扫描特定课程
bash skill.sh -c 1,2,3

# 扫描全部课程
bash skill.sh -c all

# 调试模式
bash skill.sh --debug

# 输出到文件
bash skill.sh -o result.txt
```

### 方式3：直接运行Node脚本

```bash
# 安装依赖
npm install playwright

# 运行
node scanner.js
```

## 执行流程

```
1. 登录学习通
   ↓
2. 提取用户CPI标识
   ↓
3. 发现所有课程
   ↓
4. 获取真实课程名
   ↓
5. 用户选择课程
   ↓
6. 扫描选中课程的作业
   ↓
7. 输出结果并分类
```

## 分类规则

**AI能做** (✅)：
- 纯文字写作
- 数据分析/可视化
- 代码类作业
- 案例分析
- 评论/评论

**需手动** (❌)：
- 视频拍摄/剪辑
- H5制作
- PPT制作 + 录视频
- 小组协作作业

## 参数说明

| 参数 | 说明 | 示例 |
|------|------|------|
| `-p, --phone` | 手机号（账号） | `-p 13213732826` |
| `-d, --password` | 密码 | `-d mypassword` |
| `-c, --course` | 课程序号，逗号分隔 | `-c 1,2,3` 或 `-c all` |
| `-m, --mode` | 扫描模式 | `-m scan` 或 `-m discover` |
| `-o, --output` | 输出文件 | `-o result.txt` |
| `--debug` | 调试模式 | `--debug` |
| `--clean` | 清理会话文件 | `--clean` |
| `-h, --help` | 显示帮助 | `-h` |

## 注意事项

### 🔐 安全

- **不保存密码**：账号密码仅用于本次登录，不持久化
- **会话清理**：会话文件 `.chaoxing-session.json` 包含登录态，会话结束后提示清理
- **临时文件**：所有敏感信息都是临时的

### ⚡ 性能

- **会话复用**：同一会话中通过 `storageState()` 避免重复登录
- **批量处理**：复用浏览器实例，批量处理课程
- **超时优化**：
  - 页面加载：60秒（学习通响应慢）
  - 元素等待：10秒
  - 标签点击：3秒

### 🛡️ 反爬虫

- **Referer头**：所有请求添加 `https://i.chaoxing.com/` 的Referer
- **浏览器指纹**：设置User-Agent模拟真实浏览器
- **多URL降级**：如果主URL返回404，自动尝试备用URL

### 🐛 调试

启用调试模式查看详细日志：
```bash
bash skill.sh --debug
```

调试日志包括：
- 当前课程和URL
- 发现的作业数量
- 使用的解析方式
- 错误详情

## 常见问题

### 问题1：课程名显示"stucoursemiddle"

**原因**：从链接文本提取的名称是URL片段

**解决**：脚本会自动访问详情页获取真实课程名

### 问题2：找不到作业iframe

**原因**：iframe的name属性可能变化

**解决**：脚本通过URL特征定位（最可靠）

### 问题3：点击作业标签失败

**原因**：DOM结构复杂，选择器不匹配

**解决**：脚本通过evaluate在页面中直接查找并点击

### 问题4：作业状态不匹配

**原因**：已提交的作业显示"待批阅"，不是"未交"

**解决**：脚本只过滤"未交"/"未完成"，忽略"待批阅"

### 问题5：页面加载超时

**原因**：学习通响应慢，30秒超时不够

**解决**：脚本增加超时时间到60秒

### 问题6：反爬虫机制触发（404）

**原因**：学习通检测到自动化访问

**解决**：脚本使用多URL降级策略 + 浏览器指纹伪装

## 文件结构

```
skill.sh           - 主脚本（bash）
scanner.js         - 核心逻辑（Node.js）
.chaoxing-session.json - 登录会话（临时）
```

## 依赖

- Node.js >= 18
- npm 或 yarn
- Playwright

## 示例输出

```
===== 学习通作业扫描结果（共扫描 3 门课程）=====

📘 新闻策划与写作
  [AI能做] 平时作业3.2：人物通讯类新闻写作 — 未交
  [预计10分钟]
  [❌需手动] 作业3：短视频新闻 — 未交
  [预计2小时]

📘 融合新闻学
  [AI能做] 作业1：优秀融合新闻案例分析 — 未交
  [预计15分钟]

📘 新媒体数据分析与应用
  ✓ 没有未交作业

====================

📊 总计：3 个未交作业

💡 要我帮你完成这些作业吗？直接告诉我序号就行。
⚠️ 所有作业提交会经过查重系统，建议改写后再交。
```

## 更新日志

### v1.0.0 (2026-05-31)
- ✅ 初始版本
- ✅ 支持账号密码和扫码登录
- ✅ 自动发现课程
- ✅ 结构化+文本双重解析
- ✅ 反爬虫机制
- ✅ 调试模式

## 许可

MIT License
